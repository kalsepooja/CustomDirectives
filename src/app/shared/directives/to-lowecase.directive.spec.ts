import { ToLowecaseDirective } from './to-lowecase.directive';

describe('ToLowecaseDirective', () => {
  it('should create an instance', () => {
    const directive = new ToLowecaseDirective();
    expect(directive).toBeTruthy();
  });
});
